pub use seo_domain::identity::{
    assert_scope_matches_context, build_scope_payload, derive_scope, derive_scope_from_payload,
    derive_truth_identity, normalize_applicant_profile, normalize_optional_subtype,
    scope_signature_from_payload, truth_identity_tuple, ValidatedSeoBuildIdentity,
    ValidatedSeoScope, ValidatedTruthIdentity,
};
